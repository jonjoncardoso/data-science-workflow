name: setup

# Controls when the action will run. Triggers the workflow on creation of a new project
on:
  push
    #branches:
     # - 'main'
    #paths:
    #  - '**.md'
  
# workflow: will trigger error due to this big https://github.com/cli/cli/issues/5633, but label will be created
jobs:

  create-labels:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
      
      # Create labels
      - run: gh label create blocked2🚧 --description "Task blocked by some other issue." --color E7A922;
             #gh label clone jonjoncardoso/data-science-workflow
      - run: gh label create bug2🐞 --description "Something isn't working" --color D73A4A
      #- run: echo "::set-output name=random-number::$(echo $RANDOM)"
      #- run: echo "${{ github.action_path }}" >> $GITHUB_PATH        
      #- run: goodbye.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          
          
      
